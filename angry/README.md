--- ### 🎯 2. 50 Angry Birds ```markdown # CS50G Project 6: 50 Angry Birds — The Physics Update 🎯 [![CS50G Score](https://img.shields.io/badge/Score-100%25-success?style=for-the-badge&logo=harvard)](https://cs50.harvard.edu/games/) [![Made with Lua](https://img.shields.io/badge/Made%20with-Lua-blue?style=for-the-badge&logo=lua)](https://www.lua.org) [![Framework LÖVE2D](https://img.shields.io/badge/Framework-LÖVE2D-pink?style=for-the-badge&logo=love)](https://love2d.org) Welcome to my implementation of **50 Angry Birds** for Harvard University's *CS50's Introduction to 2D Game Development*. This project builds upon the 2D rigid-body physics engine covered in Lecture 6, introducing slingshot launching physics, destructible fortress materials, smooth camera tracking, and a mid-flight split-bird powerup. This codebase received a perfect score of **100%** on grading evaluation! 🎉 --- ## 🚀 Project Overview The objective was to master `love.physics` (Box2D wrapper) to construct an interactive physics sandbox with realistic momentum, elasticity, structural collapse, and projectile splitting abilities. ### Core Features Implemented: * **Box2D Rigid-Body Simulation:** Programmed physics bodies, fixtures, joint anchors, and density values for wood, glass, stone, and target entities using `love.physics`. * **Slingshot Trajectory Launching:** Built a dynamic drag-and-release slingshot mechanism that calculates release vectors and applies impulse forces proportional to distance pulled. * **Split-Bird Mid-Flight Ability:** Implemented a special ability triggered by pressing `Spacebar` while the bird is airborne. The original bird splits into **three distinct bird projectiles** traveling at offset trajectory angles. * **Smooth Camera Pan & Tracking:** Engineered a dynamic camera system (`gCamera`) that smoothly tracks flying projectiles across wide levels to capture structural collapses. * **Destructible Fortresses:** Configured damage thresholds on structural blocks so impact forces accurately crack and destroy objects upon reaching momentum limits. --- ## 🎮 Game Controls | Control Input | Game Action | | :--- | :--- | | `Left Mouse Drag` | **Aim Slingshot** (Click and pull back on the bird) | | `Release Left Mouse` | **Launch Bird** into active playfield | | `Spacebar` / `Left Click` | **Trigger Split-Bird Ability** (Must be activated while bird is mid-flight) | | `R` | **Reset Level** / Reload Slingshot | | `Escape` | **Terminate Application** | --- ## 🛠️ Built With * **Language:** Lua * **Framework:** LÖVE2D * **Physics Engine:** Box2D (`love.physics`) --- ## 💻 Code Architecture | File | Description | | :--- | :--- | | `src/Bird.lua` | Handles rigid-body creation, trajectory impulses, and mid-flight split physics instantiation. | | `src/Obstacle.lua` | Manages destructible wood/glass/stone block health, density, and friction fixtures. | | `src/Level.lua` | Coordinates target placement, slingshot anchors, and win/loss state checks. | ### Mid-Flight Split Mechanics Under the Hood This snippet demonstrates how the single airborne bird splits into three separate rigid bodies with diverging trajectory vectors upon input activation: ```lua -- Split airborne bird into 3 projectiles mid-flight function Bird:split() if self.isAirborne and not self.hasSplit then self.hasSplit = true -- Get current velocity vector local vx, vy = self.body:getLinearVelocity() -- Instantiate two additional bird physics bodies with offset velocity angles local bird2 = Bird(self.body:getX(), self.body:getY() - 10) local bird3 = Bird(self.body:getX(), self.body:getY() + 10) bird2.body:setLinearVelocity(vx, vy - 50) bird3.body:setLinearVelocity(vx, vy + 50) table.insert(gLevel.birds, bird2) table.insert(gLevel.birds, bird3) end end
